import { StaffLoginForm } from "@/components/staff-login-form";

export default function DeliveryLoginPage() {
  return <StaffLoginForm title="RR Kitchen — Delivery Agent Login" redirectTo="/delivery" role="DELIVERY_AGENT" />;
}
